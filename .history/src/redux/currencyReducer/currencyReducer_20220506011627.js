const initialState = {
  label: "USD",
  symbol: "$",
};

export const headerReducer = (state = initialState, action) => {
  switch (action.type) {
    case :
      
      break;
  
    default:
      break;
  }
};
