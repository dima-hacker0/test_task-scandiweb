import { CHANGE_CATEGORY } from "../types";

export const mainPageReducer = (state, action) => {
  switch (action.type) {
    case CHANGE_CATEGORY:
      break;

    default:
      return state;
  }
};
